#include <stdio.h>

int main() {
    int num; // Declare an int variable

    printf("Enter an integer: ");
    scanf("%d", &num); // Read an int value from the keyboard into the variable

    printf("The entered value is: %d\n", num); // Print the variable's value

    return 0;
}